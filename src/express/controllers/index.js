'use strict';

const mainController = require(`./main`);
const myController = require(`./my`);
const offersController = require(`./offers`);

module.exports = {
  mainController,
  myController,
  offersController,
};
